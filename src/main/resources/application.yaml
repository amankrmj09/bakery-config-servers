spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amankrmj09/bakery-config-repo.git
          username: ${GITHUB_ACTOR}
          password: ${GITHUB_TOKEN}
          refresh-rate: 60

server:
  port: ${SERVER_PORT:8081}
